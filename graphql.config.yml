schema:
  - http://localhost:5000/graphql
documents:
  - ./src/graphql/**/*.graphql
generates:
  ./src/graphql/index.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo